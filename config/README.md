# Config

공개용 설정 템플릿입니다. 실제 운영 값은 넣지 마세요.

- `example.env`: 환경 변수 placeholder
- `hermes.example.yaml`: Hermes runtime profile 예시
- `provider-routing.example.yaml`: provider/model routing 정책 예시

허용 placeholder: `<YOUR_...>`, `${...}`
