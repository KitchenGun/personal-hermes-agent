# Skill Generation Prompt

Create or update a `SKILL.md` for a repeatable Hermes capability.

Include:

- Purpose
- When to use
- Inputs
- Procedure
- Outputs
- Tool boundaries
- Safety notes

Use only placeholder values such as `<YOUR_SERVICE_NAME>` or `${HERMES_MODEL}`. Do not copy operational secrets or private prompts.
